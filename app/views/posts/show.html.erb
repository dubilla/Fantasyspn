<article>
  <h2><%= @post.title %></h2>
  <% if @post.header_image.attached? %>
    <%= image_tag(url_for(@post.header_image)) %>
  <% end %>
  <section>
    <%= @post.content_html.html_safe %>
  </section>
</article>
